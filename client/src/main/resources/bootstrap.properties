spring.cloud.config.fail-fast=true
spring.cloud.config.profile=common,conf
spring.cloud.config.label=master
spring.cloud.config.discovery.enabled=false
spring.cloud.config.discovery.service-id=config-service:1.0.0
spring.cloud.bus.trace.enabled=true
spring.cloud.config.name=${configDir:NOT_SETTING}

management.security.enabled=false

eureka.client.service-url.defaultZone=http://localhost:8761/eureka



#spring.application.name=brpc.client
management.context-path=/
eureka.client.healthcheck.enabled=true
eureka.instance.preferIpAddress=true
eureka.client.registry-fetch-interval-seconds=1
eureka.client.registryFetchIntervalSeconds=1
## 心跳时间，即服务续约间隔时间（缺省为30s）
eureka.instance.lease-renewal-interval-in-seconds=5
eureka.instance.leaseRenewalIntervalInSeconds=5
# 发呆时间，即服务续约到期时间（缺省为90s）
eureka.instance.lease-expiration-duration-in-seconds=10
eureka.instance.leaseExpirationDurationInSeconds=10
ribbon.ServerListRefreshInterval=1000

security.basic.enabled=false

brpcVersion=V2.0.0-S-180820

logging.level.zipkin.reporter.AsyncReporter=error

endpoints.shutdown.enabled=true